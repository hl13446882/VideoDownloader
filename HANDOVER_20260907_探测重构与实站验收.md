# VideoDownloader 探测重构与实站验收交接

更新时间：2026-09-07 08:16 左右，本机时间。项目：`D:\VideoDownloader`。

## 1. 接手先读

**当前工作未完成，不能宣布四站全部通过，也不能把五项能力标记为完整实现。**

- 已完成一批源码修正、业务自动化测试和本地真实 WebView2 验收，随后按用户指定网址开展实站测试，发现了新问题并继续修正。
- **当前源码领先于 publish 中的程序。** 最近一批 TikTok 播放器数据读取、当前媒体优先验证、误识别过滤等修改尚未重新发布、尚未完成全量回归。
- 最新一轮 TikTok 重测在“等待导航产生新 Session”处超时，尚未验证新增的播放器数据读取是否有效。不要把“已编译”写成“已验证”。
- 用户现在要求交接文档，因此暂停继续改业务代码。交接时验收进程已退出，没有应继续等待的验收进程。
- 根目录没有 Git 仓库，`git status` 报 not a git repository。不要使用 reset、checkout 或假定存在可回滚提交。
- 原 `HANDOVER.md` 的 2026-09-05 内容仅供历史参考，里面的发布布局、会话流程和完成状态已有过时信息。

## 2. 用户最新验收要求

### 必须满足的原则

1. 同页面切视频，即便 URL 不变，也要识别新逻辑视频并启动一次新探测。
2. 同视频切清晰度、CDN、签名、临时 token，不应重建视频 Session、清空列表或反复探测。
3. 旧 Session 的 DOM、网络、JSON、验证、外置解析及排队 UI 回调，不得污染新 Session。
4. 文案必须归属于当前视频；不能拿网页 `<title>` 回写覆盖视频文案。
5. 当前 Session 所有 Discovery Source 和验证任务结束后，才可完成。不能因暂时没有有效 URL 就显示“未发现”。
6. 自动化必须调用实际业务代码；修改业务行为时同步修改测试，不能只做“源码含某字符串”的检测。
7. 代码测试通过后发布，再进行真实运行验收。失败要修正重测，不能用放宽断言获得通过。
8. **最新指示：实站不必完整下载，只下载一小部分证明可下载。** 视频轨和音轨分别验证；有 HTTP 地址或取得清单文本，不等于取得实际媒体数据。
9. 只测试用户明确提供的网址及其推荐流。不要另找未经用户指定的公开视频，也不要重访历史中无关的私密内容页面。

### A1-A5 硬条件

| 编号 | 条件 |
| --- | --- |
| A1 | 同 URL 内 A → B，仅创建一次 B 的新探测 Session。 |
| A2 | B 内 720P → 1080P、CDN1 → CDN2、签名刷新，不重置 Session 和已发现列表。 |
| A3 | A 的异步结果在 B 建立后返回，全部丢弃，包括已排入 Dispatcher 的回调。 |
| A4 | 取得视频文案后，后续 ProbePageAsync 不得使用 pageTitle 覆盖。 |
| A5 | 当前 Session 尚有 Discovery Source 或验证任务时，不得 Completed / 未发现。 |

“未发现”的正常判据是：Discovery 全结束、Candidate Validation 全结束、有效媒体数为零。任务超时/被取消应有相应提示，不能暗示已穷尽所有媒体能力。

### 用户指定的全部实站样本

| 站点/模式 | 地址及数量要求 |
| --- | --- |
| YouTube 1 | https://www.youtube.com/watch?v=FUZPqZ-fRBg&list=RDFUZPqZ-fRBg&start_radio=1&t=319s |
| YouTube 2 | https://www.youtube.com/watch?v=ePhVSep8e1c&list=RDePhVSep8e1c&start_radio=1&t=648s |
| 抖音推荐流 | https://www.douyin.com/?recommend=1 ，自动按方向键下，验收至少四条视频；关注同 URL 切换。 |
| 抖音精选 | https://www.douyin.com/jingxuan?modal_id=7660503158577286451 |
| TikTok 推荐流 | https://www.tiktok.com/ ，自动方向键下/滚动，验收至少四条视频；图文帖不计入四条视频。 |
| B 站 1 | https://www.bilibili.com/video/BV1xMtw6XEAu |
| B 站 2 | https://www.bilibili.com/video/BV1CXWuz3E7V/ |
| B 站 3 | https://www.bilibili.com/video/BV1x8g56LEsz/ |

总计应形成 14 条视频验收记录。每条至少记录：最终页面地址、逻辑身份、SessionId、切换次数、稳定性、文案来源、清晰度、音轨、采样状态码/字节数、失败原因和截图。

## 3. 当前发布版本

`D:\VideoDownloader\publish\VideoDownload\VideoDownloader.exe`

- 文件修改时间：2026-09-07 07:39:21。
- 大小：6,626,681 字节。
- SHA256：`51A82075351B532041413EA6EABB3CC621564E0619ACBB98AC344E864974AC7C`。
- 对应阶段产物：`artifacts\release-494d0c2f5047441fbeba3e7b8448267b`。
- **这是通过当时本地验收的阶段包，不是通过最新实站验收的最终包。**

实际发布目录仅有：

```text
publish/VideoDownload/
  VideoDownloader.exe
  ffmpeg/ffmpeg.exe
  ffmpeg/ffprobe.exe
  M3u8/N_m3u8DL-RE.exe
  M3u8/LICENSE
  tools/yt-dlp.exe
```

保持 framework-dependent、single-file，不附带 .NET Runtime。不要把测试工具、日志、截图、数据库、WebView2Data、obj/bin 等复制到发布目录。ffmpeg 路径仍不应出现在用户设置界面。

## 4. 已做的源码修正

这里的“已修改”不代表最新源码已经全部验收通过。

### 会话与生命周期

- 新增 `src/VideoDownloader.Core/Detection/DetectionSession.cs`：独立 Guid、工作 lease、封口、等待全部工作、取消。当前枚举为 Discovering / Validating / Completed / Cancelled，**不是用户建议的完整 Observed → Candidate → Confirmed 状态模型**。
- `IMediaDetectionPipeline` 增加 SessionId、IsCompleted、CompleteDiscoveryAsync、BeginDiscovery、UpdateCaption；新增 IDiscoveryScope。
- `UnifiedMediaPipeline` 使用会话 lease 跟踪异步验证、JSON discovery、已入队网络事件；用锁保护 Clear/发布等关键操作。
- RawNetworkEvent、NormalizedNetworkEvent、DetectedVideo 增加 SessionId；网络标准化保留该值。
- MainViewModel 在排队 UI 回调真正执行时再次检查 SessionId；完整探测等待 CompleteDiscoveryAsync。
- 同页换视频时改为清理旧视频列表；同视频传输地址变化不触发新的逻辑视频会话。
- 最新源码加入 90 秒验证预算，避免大量后台候选占满验证队列。预算耗尽后的错误提示、资源释放仍应进一步核查。

### 浏览器观察与媒体发现

- 新增 `src/VideoDownloader.Infrastructure/Browser/VideoObservationScript.cs`，供真实 WebView2 注入，不是只供测试的模拟实现。
- 修复了真机发现的安装失败：document-created 阶段 documentElement 可能为 null，现观察 document；不使用在隐藏窗口可能暂停的 requestAnimationFrame，改用 queueMicrotask。
- 从当前可见播放器关联区域获取身份和文案；清理末尾“展开/收起”；忽略部分明显隐藏播放器。
- 不再用整条媒体 URL 驱动 Session；重复观察相同待确认身份不会反复重置 1.5 秒防抖。
- 支持初始空结果完成后，真正的视频迟到出现时触发一次探测；文案后到可以独立更新，不重新开启完整探测。
- 移除无请求起始会话标记的 WebResourceResponse 发现路径；改用 CDP requestWillBeSent / responseReceived / loadingFinished 关联，并对 JSON body 设置体积和等待上限。
- 去重键与请求 URL 分离：请求保留原始签名 URL，不再把删除参数后的地址传给 ffprobe。
- 修正小 HLS/DASH 清单被尺寸过滤、JSON 已认可地址被二次形态过滤的问题；清单路径不按普通 segment 丢弃。
- 移除“ffprobe 失败但地址形态像视频，就直接当作有效结果”的回退；过滤图片格式和 attached_pic 封面流。
- 最新源码为 DOM 当前播放器地址增加独立验证名额，并约束本地聚合优先属于当前播放器的资源。**此约束对独立音轨、更多清晰度的覆盖影响尚需回归。**
- 最新加入播放器关联 React props/fiber memoizedProps、Vue props 的有限深度读取，尝试取得当前 item 的 id/desc/video 地址，解决 TikTok blob 视频。通过 `__vdObserve` 做身份观察，`__vdProbe` 一次性采集地址。**已编译，尚未取得实站通过证据；不要认定此项已修复。**

### 清单、下载、启动

- HlsManifestParser 支持独立音轨组和清晰度配对；DashManifestParser 保留 Representation ID、区分视频/音轨并配对。
- UnifiedMediaPipeline 可调用现有 IManifestResolver；M3u8DownloadAdapter 按 DASH representation 设置选择参数。
- 修复 audio-extract 被路由为普通直下、多轨清单走错后端的问题；保留原架构中的 DirectHttp / 多输入 / M3u8 三条下载路径，**没有改成所有业务一律调用 N_m3u8DL-RE**。
- 新增 `MediaAddressRenewal.cs`：403 后尝试从原始稳定页面重新解析并匹配清晰度/轨道，而不是只换 Header。对没有稳定内容地址的推荐流拒绝猜测重试，避免下载成另一条视频。
- 续取新地址后清理该任务旧分片状态；文件落盘不再删除已有同名目标或 overwrite:true。
- 部分未知轨道大小时 TotalContentLength 返回 null，不再把部分大小误当总量。
- DEMO 对未知大小的外置下载保守地在启动前拒绝；已知超过上限提前拒绝；运行时仍有限制。需要评估此取舍是否满足最终产品体验，不能宣称所有未知大小小文件都可在 DEMO 下载。
- DownloadEngine 增加文件、权限、格式、磁盘等错误分类，不再全部归为网络超时。M3u8 失败细分仍不完善。
- LicenseService 主板查询改为真正有取消/超时的异步 PowerShell CIM 读取，保持原哈希前缀；本轮没有恢复授权心跳，也没有修改服务器。

## 5. 自动化工具与证据

### 脚本入口

```powershell
Set-Location D:\VideoDownloader

# 业务回归 + verifier 编译，不发布，不启动 UI
.\scripts\verify-acceptance.ps1

# 回归通过后发布，并执行本地真实 WebView2 验收
.\scripts\verify-acceptance.ps1 -Publish -RealMachine

# 用户指定的全部实站，仅少量媒体采样
.\scripts\verify-requested-sites.ps1

# 定向回归，脚本只从用户给出的固定集合筛选
.\scripts\verify-requested-sites.ps1 -Sites tiktok
.\scripts\verify-requested-sites.ps1 -Sites youtube
.\scripts\verify-requested-sites.ps1 -Sites douyin
.\scripts\verify-requested-sites.ps1 -Sites bilibili
```

- 发布脚本 `scripts/publish-beta.ps1` 已设置 robocopy `/R:0 /W:0`，避免文件被锁时无限重试。
- `tools/VideoDownloader.Verify` 是实际 WPF + WebView2 + MainViewModel 的独立验收程序，**不是对 publish/VideoDownloader.exe 的完整 UI 自动化**。发布 exe 本身的最终启动/界面冒烟还应单独补证。
- `LocalAcceptance.cs` 使用本机 ffmpeg 生成无版权风险测试视频，HTTP 本地服务，检查同页切换、保持列表、下载结果字节数、ffprobe 轨道及移除列表不删文件。旧版本本地验收完整下载过自生成样本，实站按最新要求只采样。
- `LiveAcceptance.cs` 使用真实 WebView2/CDP 向下键；直链视频/音轨分别最多读取 64KB；拒绝 HTML/JSON/图片响应作为媒体成功。
- 对 HLS/DASH 新增 N_m3u8DL-RE `--custom-range 0-0 --skip-merge` 取首分片的验收实现，尚未取得实站验证证据。
- 目前脚本通过条件已收紧为视频和音轨均存在。早期“只有音轨也 PASS”的记录无效，见下节。

### 测试文件

- `tests/VideoDownloader.Infrastructure.Tests/DetectionAcceptanceTests.cs`：A1-A5、Discovery lease、初始视频迟到、完成后文案更新。
- `tests/VideoDownloader.Infrastructure.Tests/DetectionCancellationTests.cs`：旧外置解析结果/错误、取消。
- `tests/VideoDownloader.Infrastructure.Tests/DownloadPolicyAcceptanceTests.cs`：地址续取、禁止推荐流猜测、路由、错误类型、部分未知大小。
- `tests/VideoDownloader.Core.Tests/ManifestCoverageTests.cs`：HLS 音轨和清晰度、DASH ID/轨道选择。
- `tests/VideoDownloader.Infrastructure.Tests/UnifiedDownloadIntegrationTests.cs`：真实本地 ffmpeg/HTTP/M3u8/音轨/历史记录。

### 已有证据的准确范围

- 07:39 阶段全量：Core **33 通过**；Infrastructure **55 通过、1 跳过**。合计 **88 通过、1 跳过**。跳过的是需显式启用的在线授权服务器测试。
- 同阶段本地真实 WebView2 验收通过；曾先失败，再定位脚本安装问题修正重测通过。
- 08:07 阶段定向 DetectionAcceptanceTests + DetectionCancellationTests：**15 通过**。
- 更晚的播放器 props 提取、身份过滤、实站工具改动仅部分编译检查；**最新源码未跑完全量测试**。
- 当前证据备份：`D:\VideoDownloader\artifacts\handover-20260907\`，包含两份阶段 TRX、上轮 TikTok/B站结果 JSON、截图和最新失败日志。
- `artifacts/live-acceptance/results.json`、`artifacts/vd-verify-latest.log` 会被后续运行覆盖。备份中的 JSON 是上一轮结果，最新 TikTok重测尚未产生新结果，不能把旧 JSON 当成新一轮输出。

## 6. 实站结果，禁止混淆阶段

| 样本 | 已观察事实 | 当前结论 |
| --- | --- | --- |
| YouTube FUZPqZ-fRBg | 发现 1080P 至 144P、独立音轨；视频及音轨均 HTTP 206、读取 64KB。第一轮结束状态未完成，曾出现过期示例地址。 | 下载链路有成功证据；生命周期修正后必须重测，不能整条标最终通过。 |
| YouTube ePhVSep8e1c | 发现视频及音轨，有意义文案，小样本均 206，阶段脚本通过。 | 旧阶段通过，最新源码仍需回归。 |
| 抖音推荐四条 | 点击页面“推荐”后才进入真正推荐流。第2/3/4次切换均观察到一次 Session 变更和媒体采样成功。 | 第1条只有音轨却被旧断言误判 PASS，不能计通过；收紧断言及优先验证后重测全部四条。 |
| 抖音精选 modal_id=7660503158577286451 | 获取对应文案，但旧阶段只有音轨采样。 | 不通过；不能把音轨成功当视频成功。 |
| TikTok 四条 | 首条实际是图文帖；后续视频可播放、URL 不变、Session 切换可见，但 blob 播放器没有 DOM HTTP 地址；第4条仅取得音轨。根首页 yt-dlp 报 Unsupported URL。 | 四条视频验收未通过。新增播放器数据提取方案待验证。 |
| B站 BV1xMtw6XEAu | 页面标题已加载，截图播放器黑屏/页面未完全加载，未得到媒体。 | 不通过；需区分页面加载问题、初始会话完成时机和探测缺失。 |
| B站 BV1CXWuz3E7V | 有正确视频文案，视频/音轨分别读取 64KB，HTTP 206。 | 阶段通过。 |
| B站 BV1x8g56LEsz | 有正确视频文案，视频/音轨样本成功，阶段脚本通过。 | 阶段通过。 |

注意：小样本成功只证明当时地址和请求上下文可读，不等于完整下载、全部分片、封装或长期有效性已经验收。

## 7. 后续修正顺序

### P0：先修正导航和验收起点

最新运行在 `LiveAcceptance.cs` 的导航 Session 等待处超时，日志：`artifacts/handover-20260907/last-verifier.log`。

当前流程先 AddTab/AttachWebView，随后对同一首网址又 Navigate；脚本强制等 SessionId 再变一次，而 MainViewModel 存在同 URL、正在探测时不重建的防重逻辑。**这是待验证的直接嫌疑，不应把导航等待超时误判成 TikTok 网络失败。**

建议明确区分：文档导航/显式刷新、URL 软变更、逻辑视频变更、重复通知。通过 NavigationGeneration/NavigationCompleted 同步验收起点，而不是只比较 Host 或强制要求重复 Navigate 一定再变 Session。补一个“同 URL 刷新时旧验证仍在运行”的自动化用例。

### P0：验证身份来源和 blob 视频数据路径

1. 定向跑 TikTok，记录当前播放器祖先元素、props 键及真实 item 结构；工具已增加失败诊断字段，不要继续凭猜测扩展全局扫描。
2. 验证 playerRecord 是否找到**当前** item，而非邻近、预加载或另一个可见卡片。
3. 对 blob 模式从该 item.video 枚举地址，文案、id、媒体地址同源绑定；再交给现有验证和下载流程。
4. 若某种 props 结构不匹配，基于实际诊断做有限适配，不扫完整 React fiber 树，不把整个页面 state 的全部视频混进当前列表。
5. 添加真实 WebView2 本地 fixture：同一个 video 元素、blob currentSrc、组件数据 A→B、质量/签名变化。调用生产脚本验证，不用测试专用简化逻辑替代。
6. 从 caption fallback 升级到稳定 id 时，仍有误建新 Session 的风险，应测试并设计身份确认/别名规则。没有 id 且文案相同的同页视频仍存在识别盲区，不能承诺已彻底解决。

### P0/P1：当前媒体优先与所有异步源完整收口

- 验证 primarySlots 和 primaryUrls 限制是否让当前视频先验证，且没有误丢独立音轨、更多清晰度。
- 不要用一个“音轨已成功”或“备用分辨率已成功”跳过尚未完成的当前视频验证。
- 检查验证预算超时、CDP JSON 读取超时、取消任务、Channel backlog、页面切换和 Dispatcher 排队回调的所有释放路径。
- UI 完成提示必须读取同一 Session 的真实状态；页面不同 Session 的状态不能混读。需要针对已观察的“有结果但一直正在验证”补 UI 回归。
- 保留图片/封面误识别、失败 ffprobe 不得伪造有效 URL 的回归用例。

### P1：补齐下载/清单恢复证据

- 当前 403 恢复只覆盖能从稳定页面重新解析且匹配轨道的情况。推荐流没有可靠内容 URL 时仍保守失败，后续应保存真实视频 permalink/content id，而不是用当前首页刷新另一条视频。
- 检查 DASH representation 选择与 N_m3u8DL-RE 实际 CLI 匹配，尤其多 Period、多音轨；HLS 音轨组、音视频分离分别测实际首分片。
- 检查小样本重定向：当前直链采样实现可能直接记录 302 失败，尚未与生产下载器的重定向链完整对齐。禁止为测试方便丢 Cookie/Referer/签名。
- M3u8 错误类型仍不能把未知问题全部称作格式错误；进程取消/读取 stdout-stderr、失败后的临时文件清理还应复核。
- DEMO 外置未知大小提前拒绝属于保守策略，说明清楚；不要为了通过验收取消限制或下载完整文件后才判断。

### P1：修正验收工具自身的可信度

- 必须验证 14 条预期记录齐全、终态标记齐全、记录属于本轮运行；不能只看进程 ExitCode。
- 关闭验收窗口/异常退出可能留下上轮 JSON，甚至正常窗口关闭返回 0，不能判成功。
- 每轮使用独立 runId/输出目录，并建立 latest 指针或汇总，避免各站定向测试互相覆盖证据。
- 图文帖可跳过，但至少取得四个不同、实际播放的视频身份，不能将四次按键等同于四条视频。
- 第一条的 switches 当前没有严格限制；应把页面加载阶段与单视频稳定阶段分开计数，禁止因此放过重复探测。
- 页面标题不相同、文件名不为空，仅是弱检查；最终应记录 caption 来源和绑定的视频身份。
- 最终对 publish 中真正的主程序补一次启动/界面冒烟；不能把独立 Verify 程序跑通称作发布 exe 全 UI 已通过。

## 8. 推荐接手流程与退出条件

1. 阅读本文件、保留现场及现有修改，不先重构外围功能，不改授权服务器。
2. 先修验收起点超时和运行证据隔离，再定向验证 TikTok 当前播放器数据路径。
3. 同步更新 A1-A5、blob/组件数据、刷新、图片误过滤和采样断言测试。
4. 重跑 YouTube 两条、抖音推荐至少四条及精选一条、TikTok 至少四条、B站三条，记录失败原因。
5. 跑全量业务测试及本地真实 WebView2 验收；一项失败就修正重测。
6. 全部必需断言通过后重新发布；验证 publish 与源码构建一致，保存 SHA256 和测试 runId。
7. 对最终发布 exe 做启动检查，并对最终构建执行实站小样本回归。最终报告区分“代码测试”“真实 WebView2”“发布 exe”“实站采样”。

**退出条件：A1-A5 自动化通过，14 条实站视频记录齐全且视频/音轨小样本合格，最新发布包一致，所有异常和未覆盖范围明确写出。若网站验证/加载/网络阻塞仍存在，应明确未通过，不能修改通过条件或宣布全部完成。**
